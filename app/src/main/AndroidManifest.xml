<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.arbaelbarca.loginfirebaseemail">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".AppMain"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_new"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.activity.topup.DetailTopupSaldoActivity"></activity>
        <activity android:name=".ui.activity.topup.TopupSaldoActivity" />
        <activity android:name=".ui.activity.menulayanan.DetailFinishOrderActivity" />
        <activity android:name=".ui.activity.menulayanan.DetailOrderLayananActivity" />
        <activity android:name=".ui.activity.menulayanan.DetailJasaLayananActivity" />
        <activity android:name=".ui.activity.menulayanan.AddJasaLayananActivity" />
        <activity android:name=".ui.activity.auth.ChangePassword" />
        <activity android:name=".ui.activity.profile.EditProfileActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.activity.auth.RegisterActivity" />
        <activity
            android:name=".ui.activity.auth.LoginActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".ui.activity.SplashScreen">
            <intent-filter>
                <action android:name="home" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".services.LocationService" />

        <receiver
            android:name=".reminder.BroadcastNotif"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/key_maps" />

        <service
            android:name=".FcmFirebase.MyFirebaseMessaging"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".FcmFirebase.FirebaseIdService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>